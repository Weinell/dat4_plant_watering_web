version: '3'

services:

  reverse-proxy:
    image: nginx:latest
    container_name: reverse-proxy
    depends_on:
      - rust-backend
    volumes:
      - ./reverse-proxy/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"

  rust-backend:
    build: ./rust-backend
    volumes:
      - ./rust-backend:/usr/src/app
    ports:
      - "3000:3000"
